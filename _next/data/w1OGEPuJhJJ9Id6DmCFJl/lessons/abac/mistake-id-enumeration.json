{"pageProps":{"post":{"attributes":{"description":"Demo of ID enumeration attack and how to prevent users from accessing resources by guessing IDs."},"html":"<h1>🔴 Mistake: ID Enumeration</h1>\n<p>Even with ownership checks, there&#39;s still a vulnerability: users can discover and access resources from OTHER organizations.</p>\n<h2>The Attack</h2>\n<ol>\n<li>Log in as <code>editor@acme.com</code> (Acme Corp, orgId: 1)</li>\n<li>Your documents have IDs like 1, 2, 3</li>\n<li>Try accessing <code>/api/documents/10</code> - a Globex document!</li>\n<li>Even if you can&#39;t edit it, you can READ it</li>\n</ol>\n<h2>Why This Happens</h2>\n<p>Our ownership check only runs on UPDATE. The READ route has no org filtering:</p>\n<pre><code class=\"hljs language-typescript\"><button class=\"copy-btn\">Copy</button><span class=\"hljs-comment\">// ❌ BAD: Returns ANY document by ID</span>\nrouter.<span class=\"hljs-title function_\">get</span>(<span class=\"hljs-string\">&quot;/:id&quot;</span>, <span class=\"hljs-title function_\">async</span> (req, res) =&gt; {\n  <span class=\"hljs-keyword\">const</span> doc = <span class=\"hljs-keyword\">await</span> db.<span class=\"hljs-property\">query</span>.<span class=\"hljs-property\">documents</span>.<span class=\"hljs-title function_\">findFirst</span>({\n    <span class=\"hljs-attr\">where</span>: <span class=\"hljs-title function_\">eq</span>(documentsTable.<span class=\"hljs-property\">id</span>, <span class=\"hljs-built_in\">parseInt</span>(req.<span class=\"hljs-property\">params</span>.<span class=\"hljs-property\">id</span>)),\n  })\n  res.<span class=\"hljs-title function_\">json</span>(doc) <span class=\"hljs-comment\">// Leaks cross-org data!</span>\n})\n</code></pre><h2>The Fix: Organization Scoping</h2>\n<pre><code class=\"hljs language-typescript\"><button class=\"copy-btn\">Copy</button><span class=\"hljs-comment\">// ✅ GOOD: Only returns documents in user&#x27;s org</span>\nrouter.<span class=\"hljs-title function_\">get</span>(<span class=\"hljs-string\">&quot;/:id&quot;</span>, requireAuth, <span class=\"hljs-title function_\">async</span> (req, res) =&gt; {\n  <span class=\"hljs-keyword\">const</span> doc = <span class=\"hljs-keyword\">await</span> db.<span class=\"hljs-property\">query</span>.<span class=\"hljs-property\">documents</span>.<span class=\"hljs-title function_\">findFirst</span>({\n    <span class=\"hljs-attr\">where</span>: <span class=\"hljs-title function_\">and</span>(\n      <span class=\"hljs-title function_\">eq</span>(documentsTable.<span class=\"hljs-property\">id</span>, <span class=\"hljs-built_in\">parseInt</span>(req.<span class=\"hljs-property\">params</span>.<span class=\"hljs-property\">id</span>)),\n      <span class=\"hljs-title function_\">eq</span>(documentsTable.<span class=\"hljs-property\">orgId</span>, req.<span class=\"hljs-property\">user</span>!.<span class=\"hljs-property\">orgId</span>)\n    ),\n  })\n\n  <span class=\"hljs-keyword\">if</span> (!doc) {\n    <span class=\"hljs-keyword\">return</span> res.<span class=\"hljs-title function_\">status</span>(<span class=\"hljs-number\">404</span>).<span class=\"hljs-title function_\">json</span>({ <span class=\"hljs-attr\">error</span>: <span class=\"hljs-string\">&quot;Document not found&quot;</span> })\n  }\n\n  res.<span class=\"hljs-title function_\">json</span>(doc)\n})\n</code></pre><p>Now even if an attacker guesses valid IDs, they get 404 for other orgs&#39; documents.</p>\n<h2>Key Principle</h2>\n<blockquote>\n<p>Always scope queries to the user&#39;s authorization context.</p>\n</blockquote>\n<p>Don&#39;t just check &quot;can they do the action?&quot; Also ensure &quot;is this resource even visible to them?&quot;</p>\n","markdown":"\n# 🔴 Mistake: ID Enumeration\n\nEven with ownership checks, there's still a vulnerability: users can discover and access resources from OTHER organizations.\n\n## The Attack\n\n1. Log in as `editor@acme.com` (Acme Corp, orgId: 1)\n2. Your documents have IDs like 1, 2, 3\n3. Try accessing `/api/documents/10` - a Globex document!\n4. Even if you can't edit it, you can READ it\n\n## Why This Happens\n\nOur ownership check only runs on UPDATE. The READ route has no org filtering:\n\n```typescript\n// ❌ BAD: Returns ANY document by ID\nrouter.get(\"/:id\", async (req, res) => {\n  const doc = await db.query.documents.findFirst({\n    where: eq(documentsTable.id, parseInt(req.params.id)),\n  })\n  res.json(doc) // Leaks cross-org data!\n})\n```\n\n## The Fix: Organization Scoping\n\n```typescript\n// ✅ GOOD: Only returns documents in user's org\nrouter.get(\"/:id\", requireAuth, async (req, res) => {\n  const doc = await db.query.documents.findFirst({\n    where: and(\n      eq(documentsTable.id, parseInt(req.params.id)),\n      eq(documentsTable.orgId, req.user!.orgId)\n    ),\n  })\n\n  if (!doc) {\n    return res.status(404).json({ error: \"Document not found\" })\n  }\n\n  res.json(doc)\n})\n```\n\nNow even if an attacker guesses valid IDs, they get 404 for other orgs' documents.\n\n## Key Principle\n\n> Always scope queries to the user's authorization context.\n\nDon't just check \"can they do the action?\" Also ensure \"is this resource even visible to them?\"\n","slug":"mistake-id-enumeration","title":"Mistake Id Enumeration","section":"Attribute-Based Access Control","icon":"sliders-h","filePath":"/home/runner/work/fem-permission-systems-that-scale/fem-permission-systems-that-scale/lessons/03-abac/C-mistake-id-enumeration.md","nextSlug":"/fem-permission-systems-that-scale/lessons/abac/organization-scoping","prevSlug":"/fem-permission-systems-that-scale/lessons/abac/ownership-permissions"}},"__N_SSG":true}