{"pageProps":{"post":{"attributes":{"description":"Implement the builder pattern for collecting permission rules."},"html":"<h1>Builder Pattern</h1>\n<p>The builder collects rules via <code>allow()</code> and <code>deny()</code> calls.</p>\n<h2>Live Code: createPermissions Function</h2>\n<pre><code class=\"hljs language-typescript\"><button class=\"copy-btn\">Copy</button><span class=\"hljs-comment\">// packages/permissions/src/builder.ts</span>\n\n<span class=\"hljs-keyword\">import</span> { <span class=\"hljs-title class_\">Rule</span>, <span class=\"hljs-title class_\">PermissionBuilder</span>, <span class=\"hljs-title class_\">PermissionContext</span> } <span class=\"hljs-keyword\">from</span> <span class=\"hljs-string\">&quot;./types&quot;</span>\n<span class=\"hljs-keyword\">import</span> { <span class=\"hljs-title class_\">PermissionsImpl</span> } <span class=\"hljs-keyword\">from</span> <span class=\"hljs-string\">&quot;./permissions&quot;</span>\n\n<span class=\"hljs-keyword\">export</span> <span class=\"hljs-keyword\">function</span> createPermissions&lt;<span class=\"hljs-title class_\">TUser</span>, <span class=\"hljs-title class_\">TSubject</span> <span class=\"hljs-keyword\">extends</span> <span class=\"hljs-built_in\">string</span> = <span class=\"hljs-built_in\">string</span>&gt;(\n  <span class=\"hljs-attr\">builder</span>: <span class=\"hljs-title class_\">PermissionBuilder</span>&lt;<span class=\"hljs-title class_\">TUser</span>, <span class=\"hljs-title class_\">TSubject</span>&gt;,\n  <span class=\"hljs-attr\">user</span>: <span class=\"hljs-title class_\">TUser</span>\n) {\n  <span class=\"hljs-keyword\">const</span> <span class=\"hljs-attr\">rules</span>: <span class=\"hljs-title class_\">Rule</span>&lt;<span class=\"hljs-title class_\">TSubject</span>&gt;[] = []\n\n  <span class=\"hljs-comment\">// allow() can take conditions, fields, or both</span>\n  <span class=\"hljs-keyword\">const</span> <span class=\"hljs-attr\">allow</span>: <span class=\"hljs-title class_\">AllowFn</span>&lt;<span class=\"hljs-title class_\">TSubject</span>&gt; = <span class=\"hljs-function\">(<span class=\"hljs-params\">\n    action,\n    subject,\n    conditionsOrFields,\n    fields\n  </span>) =&gt;</span> {\n    <span class=\"hljs-keyword\">const</span> <span class=\"hljs-attr\">rule</span>: <span class=\"hljs-title class_\">Rule</span>&lt;<span class=\"hljs-title class_\">TSubject</span>&gt; = {\n      <span class=\"hljs-attr\">type</span>: <span class=\"hljs-string\">&quot;allow&quot;</span>,\n      action,\n      subject,\n    }\n\n    <span class=\"hljs-comment\">// Determine if second param is conditions or fields</span>\n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-title class_\">Array</span>.<span class=\"hljs-title function_\">isArray</span>(conditionsOrFields)) {\n      rule.<span class=\"hljs-property\">fields</span> = conditionsOrFields\n    } <span class=\"hljs-keyword\">else</span> <span class=\"hljs-keyword\">if</span> (conditionsOrFields) {\n      rule.<span class=\"hljs-property\">conditions</span> = conditionsOrFields\n      <span class=\"hljs-keyword\">if</span> (fields) {\n        rule.<span class=\"hljs-property\">fields</span> = fields\n      }\n    }\n\n    rules.<span class=\"hljs-title function_\">push</span>(rule)\n  }\n\n  <span class=\"hljs-comment\">// deny() only takes conditions</span>\n  <span class=\"hljs-keyword\">const</span> <span class=\"hljs-attr\">deny</span>: <span class=\"hljs-title class_\">DenyFn</span>&lt;<span class=\"hljs-title class_\">TSubject</span>&gt; = <span class=\"hljs-function\">(<span class=\"hljs-params\">action, subject, conditions</span>) =&gt;</span> {\n    rules.<span class=\"hljs-title function_\">push</span>({\n      <span class=\"hljs-attr\">type</span>: <span class=\"hljs-string\">&quot;deny&quot;</span>,\n      action,\n      subject,\n      conditions,\n    })\n  }\n\n  <span class=\"hljs-comment\">// Execute the builder</span>\n  <span class=\"hljs-keyword\">const</span> <span class=\"hljs-attr\">context</span>: <span class=\"hljs-title class_\">PermissionContext</span>&lt;<span class=\"hljs-title class_\">TUser</span>&gt; = { user }\n  <span class=\"hljs-title function_\">builder</span>(allow, deny, context)\n\n  <span class=\"hljs-comment\">// Return the Permissions instance</span>\n  <span class=\"hljs-keyword\">return</span> <span class=\"hljs-keyword\">new</span> <span class=\"hljs-title class_\">PermissionsImpl</span>&lt;<span class=\"hljs-title class_\">TSubject</span>&gt;(rules)\n}\n</code></pre><h2>Usage Example</h2>\n<pre><code class=\"hljs language-typescript\"><button class=\"copy-btn\">Copy</button><span class=\"hljs-keyword\">const</span> permissions = createPermissions&lt;<span class=\"hljs-title class_\">User</span>, <span class=\"hljs-string\">&quot;Document&quot;</span> | <span class=\"hljs-string\">&quot;User&quot;</span>&gt;(\n  <span class=\"hljs-function\">(<span class=\"hljs-params\">allow, deny, { user }</span>) =&gt;</span> {\n    <span class=\"hljs-title function_\">allow</span>(<span class=\"hljs-string\">&quot;read&quot;</span>, <span class=\"hljs-string\">&quot;Document&quot;</span>)\n    <span class=\"hljs-title function_\">allow</span>(<span class=\"hljs-string\">&quot;update&quot;</span>, <span class=\"hljs-string\">&quot;Document&quot;</span>, { <span class=\"hljs-attr\">authorId</span>: user.<span class=\"hljs-property\">id</span> })\n    <span class=\"hljs-title function_\">allow</span>(<span class=\"hljs-string\">&quot;read&quot;</span>, <span class=\"hljs-string\">&quot;Document&quot;</span>, [<span class=\"hljs-string\">&quot;title&quot;</span>, <span class=\"hljs-string\">&quot;content&quot;</span>])\n    <span class=\"hljs-title function_\">deny</span>(<span class=\"hljs-string\">&quot;update&quot;</span>, <span class=\"hljs-string\">&quot;Document&quot;</span>, { <span class=\"hljs-attr\">status</span>: <span class=\"hljs-string\">&quot;archived&quot;</span> })\n  },\n  currentUser\n)\n</code></pre><p>Now let&#39;s implement the permission checking logic.</p>\n","markdown":"\n# Builder Pattern\n\nThe builder collects rules via `allow()` and `deny()` calls.\n\n## Live Code: createPermissions Function\n\n```typescript\n// packages/permissions/src/builder.ts\n\nimport { Rule, PermissionBuilder, PermissionContext } from \"./types\"\nimport { PermissionsImpl } from \"./permissions\"\n\nexport function createPermissions<TUser, TSubject extends string = string>(\n  builder: PermissionBuilder<TUser, TSubject>,\n  user: TUser\n) {\n  const rules: Rule<TSubject>[] = []\n\n  // allow() can take conditions, fields, or both\n  const allow: AllowFn<TSubject> = (\n    action,\n    subject,\n    conditionsOrFields,\n    fields\n  ) => {\n    const rule: Rule<TSubject> = {\n      type: \"allow\",\n      action,\n      subject,\n    }\n\n    // Determine if second param is conditions or fields\n    if (Array.isArray(conditionsOrFields)) {\n      rule.fields = conditionsOrFields\n    } else if (conditionsOrFields) {\n      rule.conditions = conditionsOrFields\n      if (fields) {\n        rule.fields = fields\n      }\n    }\n\n    rules.push(rule)\n  }\n\n  // deny() only takes conditions\n  const deny: DenyFn<TSubject> = (action, subject, conditions) => {\n    rules.push({\n      type: \"deny\",\n      action,\n      subject,\n      conditions,\n    })\n  }\n\n  // Execute the builder\n  const context: PermissionContext<TUser> = { user }\n  builder(allow, deny, context)\n\n  // Return the Permissions instance\n  return new PermissionsImpl<TSubject>(rules)\n}\n```\n\n## Usage Example\n\n```typescript\nconst permissions = createPermissions<User, \"Document\" | \"User\">(\n  (allow, deny, { user }) => {\n    allow(\"read\", \"Document\")\n    allow(\"update\", \"Document\", { authorId: user.id })\n    allow(\"read\", \"Document\", [\"title\", \"content\"])\n    deny(\"update\", \"Document\", { status: \"archived\" })\n  },\n  currentUser\n)\n```\n\nNow let's implement the permission checking logic.\n","slug":"builder-pattern","title":"Builder Pattern","section":"Build Your Own","icon":"hammer","filePath":"/home/runner/work/fem-permission-systems-that-scale/fem-permission-systems-that-scale/lessons/06-build-your-own/C-builder-pattern.md","nextSlug":"/fem-permission-systems-that-scale/lessons/build-your-own/permission-checking","prevSlug":"/fem-permission-systems-that-scale/lessons/build-your-own/core-types"}},"__N_SSG":true}